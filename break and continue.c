#include <stdio.h>
int main()
{
	int m;
	printf("Enter the value of m and click enter\n\n");
	scanf("%d", &m);
	if (m == 56)
	{
		int v;
		for (v = 0; v < 10; v++)
		{
		    if (v == 4);
			break;
			else

			printf("%d", v);

			v = v + 2;

			}
		}
	}

